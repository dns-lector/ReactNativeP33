import { StyleSheet } from "react-native";

const CalcStyle = StyleSheet.create({
    pageContainer: {
        flex: 1,
    },
    pageTitle: {
        fontWeight: 600,
    }
});

export default CalcStyle;
