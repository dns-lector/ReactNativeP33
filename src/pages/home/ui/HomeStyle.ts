import { StyleSheet } from "react-native";

const HomeStyle = StyleSheet.create({
    pageContainer: {
        flex: 1,
    },
    pageTitle: {
        fontWeight: 600,
    }
});

export default HomeStyle;
